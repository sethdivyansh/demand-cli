import React from 'react';

/**
 * Overview default page (root of /dashboard/overview).
 * This file is required so Next.js can render the route
 */
export default function OverviewPage() {
  return null; // Layout will handle slot rendering
}
